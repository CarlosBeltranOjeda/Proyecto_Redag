services:
  - type: web
    name: Proyecto_Redag
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn redag_web.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0